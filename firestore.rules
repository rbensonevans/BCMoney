rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * BCMONEY SECURITY RULES
     * 
     * Implements a direct User-Ownership Silo model.
     * Access is granted to any path that starts with the user's authenticated UID.
     */

    function isSignedIn() {
      return request.auth != null;
    }

    // Silo: User Profiles
    // Matches /user_profiles/{userId} and all sub-paths
    match /user_profiles/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
      
      match /{allPaths=**} {
        allow read, write: if isSignedIn() && request.auth.uid == userId;
      }
    }

    // Silo: Accounts
    // Matches /accounts/{userId} and all sub-paths (including /transactions)
    match /accounts/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
      
      match /{allPaths=**} {
        allow read, write: if isSignedIn() && request.auth.uid == userId;
      }
    }
  }
}