rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * BCMONEY SECURITY RULES
     * 
     * Implements a direct User-Ownership Silo model.
     * Access is granted to any path that starts with the user's authenticated UID.
     */

    function isSignedIn() {
      return request.auth != null;
    }

    // Silo: User Profiles
    match /user_profiles/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
      
      // Explicitly allow all sub-collections (balances, recipients, etc.)
      match /{allPaths=**} {
        allow read, write: if isSignedIn() && request.auth.uid == userId;
      }
    }

    // Silo: Accounts
    match /accounts/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
      
      // Explicitly allow all sub-collections (transactions, etc.)
      match /{allPaths=**} {
        allow read, write: if isSignedIn() && request.auth.uid == userId;
      }
    }
  }
}